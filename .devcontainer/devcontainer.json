{
  "name": "Jamabandi Genie",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "extensions": [
    "ms-python.python"
  ],
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && streamlit run streamlit_app.py || true",
  "forwardPorts": [8501]
}
